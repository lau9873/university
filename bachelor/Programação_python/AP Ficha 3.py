# =============================================================================
# EX1
# =============================================================================
def media_arit(x):
    n=len(x)
    if n==0:
        return 0
    soma=0
    for a in x:
        soma=soma+a
    return soma/n

# =============================================================================
# EX2
# =============================================================================
def media_geom(x):
    n=len(x)
    if n==0:
        return 0
    mult=1
    for a in x:
        mult=mult*a
    return mult**(1/n)

# =============================================================================
# EX3
# =============================================================================
def desvio_padrao(x):
    n=len(x)
    if n>1:
        media=media_arit(x)
        soma=0
        for a in x:
            soma=(a-media)**2+soma
        desvio=(1/(n-1)*(soma))**(1/2)
        return desvio
 
# =============================================================================
# EX4
# =============================================================================   
def prod_interno(x,y):
    prod=0
    for a in range (0,len(x)):
        prod=x[a]*y[a]+prod
    return prod

# =============================================================================
# EX5
# =============================================================================
def posicao(x,v):
    for a in range (0,len(v)):
        p=0
        if v[a]==x:
            p=a+1
            return p
        if v[a]!=x:
            p=-1
    return p

# =============================================================================
# EX6
# =============================================================================
def posMin(v):
    minimo=0
    if len(v)==0:
        return -1
    for i in range(1,len(v)):
        if v[i]<v[minimo]:
            minimo=i
    return minimo+1     
 
# =============================================================================
# EX7
# =============================================================================      
def intervalo(x,a,b):
    contador=0
    for i in range (len(x)):
        if x[i]>=a and x[i]<=b:
            contador=contador+1
            i=i+1
        else:
            contador=contador
    return contador
   
# =============================================================================
# EX8
# =============================================================================         
def capicua(x):
    n=len(x)
    for i in range(n//2):
        if x[i]!=x[n-1-i]:
            return 'false'
        return 'true'

# =============================================================================
# EX9
# =============================================================================
def equimedia(x):
    n=len(x)
    for i in range(0,n//2):
        x[i]=(x[i]+x[n-1-i])/2
    for i in range(0,n//2):
        x.remove(x[n-1-i])
    return x

# =============================================================================
# EX10
# =============================================================================
def rotacao(x):
    n=len(x)
    if n>=1:
        x=x
    x.insert(0,x[n-1])
    x.pop(n)
    return x

# =============================================================================
# EX11
# =============================================================================
def lerseqfiltra():
    n=int(input())
    x=[]
    while n!=0:
        if n>0:
           x.append(n) 
        n=int(input())
    for i in range(len(x)):
        print(x[i])
        
# =============================================================================
# EX12
# =============================================================================
def contagem(v,a:int,b:int):
    d=0
    while d<=(b-a):
        s=0
        for i in range(len(v)):
            if a+d==v[i]:
                s=s+1
        print(s)
        d=d+1

# =============================================================================
# EX13
# =============================================================================
def trocas(v,k):
    s=0
    for i in range(k):
        if v[i]<v[i+1]:
            p=v[i]
            l=v[i+1]
            v[i]=l
            v[i+1]=p
            s=s+1
    print(s)
            
# =============================================================================
# EX14
# =============================================================================
def bubblesort(v):
    for k in range(2,len(v)):
        trocas(v,k)
    return v



